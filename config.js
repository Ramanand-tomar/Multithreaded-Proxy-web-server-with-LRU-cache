module.exports = {
  PORT: 8080,                 // Proxy server port
  CACHE_LIMIT: 50,            // Max number of items in cache
  TARGET_HOST: "", // Default host to proxy
  TARGET_PORT: 80             // Default target port
};
